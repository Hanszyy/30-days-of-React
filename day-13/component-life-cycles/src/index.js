import React from 'react';
import ReactDOM from 'react-dom';
import LifecycleDemo from './lifecycleDemo'; // Make sure to import the LifecycleDemo component

ReactDOM.render(
  <React.StrictMode>
    <LifecycleDemo />
  </React.StrictMode>,
  document.getElementById('root')
);
